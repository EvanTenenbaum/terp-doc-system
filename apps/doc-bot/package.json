{
  "name": "doc-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright-based documentation automation for TERP",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/runner/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linter configured'",
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "docs:auth": "tsx src/cli/auth.ts",
    "docs:seed": "tsx src/cli/seed.ts",
    "docs:run": "tsx src/cli/run.ts",
    "docs:report": "tsx src/cli/report.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.6.0",
    "typescript": "^5.3.3"
  }
}
